class ModelVariants {
  String id;
  String product_id;
  String nom;
  String price;
  String product_id_index;
  String image;
  String message;
  ModelVariants(this.id, this.product_id, this.nom, this.price,
      this.product_id_index, this.image, this.message);
}
